# Analysis pipeline (local; needs the IEEE-CIS dataset)
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
lightgbm>=4.3
networkx>=3.2
pyarrow>=15.0
matplotlib>=3.8

# Web layer: dashboard + Razorpay webhook receiver
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1

# Shared
requests>=2.31
python-dotenv>=1.0

# Tests
pytest>=8.0
httpx>=0.27
# Already installed transitively by uvicorn[standard]; declared because
# tests/test_coldstart.py imports it directly and an undeclared transitive
# import is a break waiting for a dependency bump. Installs nothing new.
pyyaml>=6.0
